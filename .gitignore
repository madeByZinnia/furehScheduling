
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Node / build
node_modules/
dist/
*.local
.wrangler/
coverage/

# Local Worker secrets for `wrangler dev` — never commit
.dev.vars

# Git worktrees live here (inside the repo, ignored)
.worktrees/
